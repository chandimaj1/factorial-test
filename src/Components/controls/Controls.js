/**
 * The Controls component hosts all the controls for quick,
 *  easy and contained controls import for any usecase
 */
import Input from "./Input";
import Autocomplete from "./Autocomplete";
import Button from "./Button";

const Controls = {
    Input,
    Autocomplete,
    Button,
}

export default Controls;
